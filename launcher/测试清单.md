# Claude Code 启动器 - 测试清单

## 📋 功能测试

### 1️⃣ 依赖安装测试

#### 测试步骤
```bash
双击：安装依赖.bat
```

#### 预期结果
- ✅ 成功安装 pywin32
- ✅ 成功安装 psutil
- ✅ 显示"依赖安装完成"

#### 验证命令
```python
python -c "import win32gui, psutil; print('✓ 依赖安装成功')"
```

---

### 2️⃣ 锁文件检测测试

#### 测试步骤A：MCP 未运行
```bash
# 1. 确保 MCP 未运行
# 2. 双击：测试锁文件检测.bat
```

#### 预期结果A
```
✗ 锁文件不存在 → Houdini MCP 未运行
启动器行为：
  - 'Houdini MCP 模式' 按钮可用
```

#### 测试步骤B：MCP 正在运行
```bash
# 1. 启动 Houdini MCP
# 2. 双击：测试锁文件检测.bat
```

#### 预期结果B
```
✓ 锁文件存在 → Houdini MCP 正在运行
启动器行为：
  - 'Houdini MCP 模式' 按钮将被禁用
```

#### 验证锁文件
```bash
# 路径
C:\Users\<用户名>\.claude\plugins\houdini-mcp\.running.lock

# 内容应包含
pid=<进程ID>
started=<时间戳>
```

---

### 3️⃣ 启动器 GUI 测试

#### 测试步骤
```bash
双击：启动 Claude Code.bat
```

#### 预期结果

**场景1：MCP 未运行**
```
状态：✅ 就绪，可以启动
按钮：
  - 🎨 Houdini MCP 模式 [可用/蓝色]
  - 📝 普通模式 [可用/绿色]
```

**场景2：MCP 正在运行**
```
状态：⚠️ Houdini MCP 已有实例运行（锁文件检测）
按钮：
  - 🎨 Houdini MCP 模式 [禁用/灰色] (已有实例运行)
  - 📝 普通模式 [可用/绿色]
```

**场景3：插件未安装**
```
状态：⚠️ Houdini MCP 插件未安装
按钮：
  - 🎨 Houdini MCP 模式 [禁用/灰色] (插件未安装)
  - 📝 普通模式 [可用/绿色]
```

---

### 4️⃣ Houdini MCP 模式测试

#### 测试步骤
```bash
1. 双击：启动 Claude Code.bat
2. 点击：🎨 Houdini MCP 模式
3. 确认对话框中点击"是"
4. 等待 Claude Code 启动
```

#### 预期结果
- ✅ Claude Code 启动成功
- ✅ 窗口标题：`Claude-Houdini MCP`
- ✅ 锁文件被创建：`.running.lock`
- ✅ Houdini MCP 插件已启用

#### 验证步骤
```bash
# 1. 检查窗口标题
任务栏应显示：Claude-Houdini MCP

# 2. 检查锁文件
双击：测试锁文件检测.bat
应显示：✓ 锁文件存在

# 3. 尝试启动第二个实例
双击：启动 Claude Code.bat
按钮应该：灰色禁用
```

---

### 5️⃣ 普通模式测试

#### 测试步骤
```bash
# 启动第1个实例
1. 双击：启动 Claude Code.bat
2. 点击：📝 普通模式
3. 确认

# 启动第2个实例
4. 再次双击：启动 Claude Code.bat
5. 点击：📝 普通模式
6. 确认

# 启动第3个实例
7. 再次双击：启动 Claude Code.bat
8. 点击：📝 普通模式
9. 确认
```

#### 预期结果
```
任务栏应显示：
[Claude-普通-0] [Claude-普通-1] [Claude-普通-2]
```

#### 验证
- ✅ 3个窗口都能启动
- ✅ 窗口标题编号递增：0、1、2
- ✅ Houdini MCP 插件已禁用
- ✅ 每个窗口独立运行

---

### 6️⃣ 单实例限制测试

#### 测试步骤
```bash
# 1. 启动第1个 Houdini MCP
双击：启动 Claude Code.bat → MCP 模式

# 2. 尝试启动第2个 Houdini MCP
双击：启动 Claude Code.bat
```

#### 预期结果
```
启动器界面：
- 状态：⚠️ Houdini MCP 已有实例运行
- MCP 按钮：灰色禁用（已有实例运行）
- 普通按钮：绿色可用

如果强行点击 MCP 按钮：
显示警告：
"检测到 Houdini MCP 锁文件存在。
已有 Houdini MCP 实例在运行..."
```

#### 验证
- ✅ 无法启动第2个 MCP
- ✅ 普通模式仍可启动
- ✅ 真正实现单实例限制

---

### 7️⃣ 混合场景测试

#### 测试步骤
```bash
# 1. 启动 Houdini MCP
启动 Claude Code.bat → MCP 模式

# 2. 启动普通模式 x3
启动 Claude Code.bat → 普通模式（重复3次）

# 3. 查看任务栏
```

#### 预期结果
```
任务栏应显示4个 Claude Code 窗口：
[Claude-Houdini MCP]
[Claude-普通-0]
[Claude-普通-1]
[Claude-普通-2]
```

#### 验证
- ✅ MCP + 多个普通模式共存
- ✅ 可以通过标题快速识别
- ✅ 每个窗口独立工作

---

### 8️⃣ 清理和恢复测试

#### 测试步骤A：正常关闭
```bash
1. 启动 Houdini MCP
2. 正常关闭 Claude Code 窗口
3. 双击：测试锁文件检测.bat
```

#### 预期结果A
```
✗ 锁文件不存在
→ 锁文件已自动清理 ✅
```

#### 测试步骤B：异常崩溃模拟
```bash
1. 启动 Houdini MCP
2. 手动创建假锁文件（模拟崩溃残留）
3. 双击：启动 Claude Code.bat
```

#### 预期结果B
```
- 检测到锁文件
- MCP 按钮禁用
- 提示：手动删除残留锁文件
```

#### 手动清理
```bash
# 删除残留锁文件
路径：C:\Users\<用户名>\.claude\plugins\houdini-mcp\.running.lock
删除后，启动器应恢复正常
```

---

### 9️⃣ 编号持久化测试

#### 测试步骤
```bash
# 1. 启动3个普通模式
启动 → 普通-0
启动 → 普通-1
启动 → 普通-2

# 2. 关闭所有窗口

# 3. 重启系统

# 4. 再次启动普通模式
启动 → 应该是 普通-3 ✅
```

#### 预期结果
- ✅ 编号不会重置
- ✅ 继续从 3 开始递增

#### 重置编号（可选）
```bash
# 删除计数器文件
路径：C:\Users\<用户名>\.claude\.normal_instance_counter

# 删除后，编号从 0 开始
```

---

### 🔟 窗口标题功能降级测试

#### 测试步骤
```bash
# 1. 卸载 pywin32
pip uninstall pywin32 -y

# 2. 启动 Claude Code
双击：启动 Claude Code.bat → 任意模式
```

#### 预期结果
- ✅ Claude Code 正常启动
- ⚠️ 窗口标题未修改（使用默认标题）
- ✅ 其他功能不受影响
- ✅ 不报错、不崩溃

#### 恢复
```bash
双击：安装依赖.bat
```

---

## ✅ 测试通过标准

### 核心功能
- [ ] 依赖安装成功
- [ ] 锁文件检测正常
- [ ] 启动器 GUI 正常显示

### MCP 模式
- [ ] 能够启动 Houdini MCP
- [ ] 窗口标题设置成功
- [ ] 锁文件自动创建
- [ ] 单实例限制生效
- [ ] 关闭后锁文件自动清理

### 普通模式
- [ ] 可以多开
- [ ] 窗口标题自动编号
- [ ] 编号持久化
- [ ] MCP 插件已禁用

### 混合场景
- [ ] MCP + 普通模式共存
- [ ] 不同实例可区分

### 异常处理
- [ ] 残留锁文件警告
- [ ] 缺少依赖时降级

---

## 📝 测试报告模板

```
测试日期：____/____/____
测试人员：__________
Python 版本：__________
系统版本：__________

功能测试结果：
✅ 依赖安装：通过/失败
✅ 锁文件检测：通过/失败
✅ MCP 单实例：通过/失败
✅ 普通多开：通过/失败
✅ 窗口标题：通过/失败
✅ 编号持久化：通过/失败

发现问题：
1. _______________
2. _______________
3. _______________

建议改进：
1. _______________
2. _______________
```

---

**版本：** v2.0
**创建日期：** 2026-03-16
